import React, { Component } from 'react'

class SearchPage extends Component {
    render() {
        return (
            <div>Logamani's Peers Search Page</div>
        );
    };
};

export default SearchPage;